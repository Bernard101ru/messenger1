# Используйте официальный образ Golang как базовый образ
FROM golang:latest

# Создайте и переключитесь в рабочую директорию
WORKDIR /app

# Скопируйте все файлы в текущую директорию образа
COPY . .

# Соберите приложение
RUN go build -o myapp

# Укажите порт, который приложение будет слушать
EXPOSE 8080

# Команда для запуска приложения при запуске контейнера
CMD ["./myapp"]